<%= form_for(@collector) do |f| %>
  <% if @collector.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@collector.errors.count, "error") %> prohibited this collector from being saved:</h2>

      <ul>
      <% @collector.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :avatar, :class => "collector-form-label" %><br />
    <%= f.file_field :avatar %>
  </div>

  <div class="field">
    <%= f.label :headshot_url, :class => "collector-form-label" %><br />
    <%= f.text_field :headshot_url %>
  </div>

  <div class="field">
    <%= f.label :name, :class => "collector-form-label" %><br />
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :email, :class => "collector-form-label" %><br />
    <%= f.text_field :email %>
  </div>

  <div class="field">
    <%= f.label :password_digest, :class => "collector-form-label" %><br />
    <%= f.text_field :password_digest %>
  </div>

  <div class="actions">
    <%= f.submit "SUBMIT" %>
    <%= link_to "NEVER MIND", root_path, class: 'button' %>
  </div>
<% end %>
